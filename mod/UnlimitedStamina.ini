; Unlimited Stamina 0.1.0 for Crimson Desert 2.03.00 (exe 1.0.0.2944).
;
; This build is a probe. It changes nothing in the game. It writes a report of
; what the running data tables hold so the mod can be built against facts
; rather than against a reading of the files, and the settings that will do the
; actual work are not here yet.
;
; The log is UnlimitedStamina.log, next to the plugin in bin64. The previous
; sessions are UnlimitedStamina.01.log upwards, newest first.

[settings]

; Write the report. 1 is on, and with it off this build does nothing at all.
Probe=1
